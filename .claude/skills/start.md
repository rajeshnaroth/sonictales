---
name: start
description: Start the SonicTales dev server (Vite on port 3000)
user_invocable: true
---

Start the Vite dev server by running `npm run dev` in the background using the Bash tool with `run_in_background: true`. Tell the user the server is starting on http://localhost:3000.
